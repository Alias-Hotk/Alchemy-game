name: CI

on: workflow_dispatch
 
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Форматирование кода
        run: black .

      - name: Статический анализ кода
        run: pylint .

